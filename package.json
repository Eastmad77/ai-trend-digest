{
  "name": "ai-trend-digest",
  "version": "1.0.0",
  "description": "Automated AI news digest that self-publishes daily for ad revenue.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "node scripts/build.js && npx live-server site --port=4321",
    "format": "npx prettier --write ."
  },
  "dependencies": {
    "date-fns": "^4.1.0",
    "dotenv": "^16.4.5",
    "openai": "^4.58.0",
    "rss-parser": "^3.13.0",
    "slugify": "^1.6.6",
    "undici": "^6.19.8"
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  }
}
